package drools;

import io.github.hephaestusmetrics.model.metrics.Metric;

dialect  "mvel"

rule "metric1"
    when
        Metric(name="xd", value>0)
    then
        System.out.println("Rule fired");
end
